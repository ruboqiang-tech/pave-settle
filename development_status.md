# 最近开发状态

更新时间：2026-06-28

## 本轮扫描范围

| 范围 | 结论 |
| --- | --- |
| 说明文件 | 已扫描根目录和 `pave-settle/` 下现有 Markdown/AGENTS 文件清单；成本管理说明按用户要求不改。 |
| 业务代码 | 已核对路由、schema、数据中心、附件、项目、合同、结算、收款、发票、报表、总包汇总相关代码。 |
| 成本管理 | 用户明确要求先不管，未改 `成本管理模块说明.md`，也未展开 `src/views/costs` 和成本服务说明。 |
| 自动化测试 | 已执行 `npm test`。 |

## 当前实现状态

| 模块 | 当前事实 |
| --- | --- |
| 路由 | 首页、项目、总包、结算、收款、报表、数据中心为本轮核对入口；成本入口存在；路由守卫已解禁，无库时仅进行页面内局部警告，不强制重定向或禁用侧边栏。 |
| 数据库 | `src/services/db-schema.ts` 管理 schema，当前版本 `13`。 |
| 存储 | 主业务库（pave.db）与全局资产库（global-assets.db）文件分别挂载。去除了浏览器文件句柄 IndexedDB 模式，仅保留直连本地硬盘的开发桥模式。移除了临时内存库初始化兜底，在无数据库物理文件时，内存中的 Database 实例保持为 `null`，通过包装层安全预防应用崩溃，并在界面展示挂载警告。 |
| 数据中心 | 支持分别设置主业务库物理路径、全局资产库物理路径、默认位置，并集成系统原生文件选择对话框（PowerShell）支持一键点击选择文件。支持断开数据库连接。 |
| 备份 | 系统备份管理逻辑已精简，改为异地手工成对拷贝备份提示。 |
| 附件 | 合同附件和结算附件写入 SQLite `file_data`。 |
| 结算链 | 结算保存、状态变更、合同 BOQ 变更会触发项目结算链重算。 |
| 报表 | 首页、报表中心、总包汇总使用业务快照和 analytics 服务计算。 |

## 验证结果

```text
npm test
Test Files  59 passed (59)
Tests       337 passed (337)
Duration    104.51s
```

## 已整理文档

| 文档 | 本轮处理 |
| --- | --- |
| `../README.md` | 新增仓库文档总入口。 |
| `README.md` | 重写为应用技术入口。 |
| `development_status.md` | 新增最近状态文档。 |
| `../TODO.md` | 收敛为当前待办。 |
| `../Web+SQLite去Tauri改造交接文档.md` | 更新为数据库管理专项交接。 |
| `../数据库管理重构完整交接方案.md` | 明确历史归档定位。 |
| `../multi_user_migration_plan.md` | 明确远期规划定位。 |
| `AGENTS.md` | 结构化协作说明。 |
| `成本管理模块说明.md` | 按用户要求未改。 |

## 仍需人工验证

1. 真实浏览器回归：项目、合同、BOQ、结算、收款、发票、报表、总包汇总、数据中心。
2. 数据中心路径切换闭环：物理数据库路径修改保存、默认位置、浏览器文件选择与断开。
3. 附件预览和下载：合同附件、结算附件都要抽查。
4. `/api/db` 开发桥边界：确认只用于开发机，不作为多人共享写库入口。
5. 构建包体积：必要时继续按路由拆包。

## 工作区注意

本轮开始前工作区已有多处未提交代码改动和未跟踪文件。文档整理只修改说明文件，不回退既有代码改动。
