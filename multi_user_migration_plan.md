# 多用户与云端协同远期规划

定位：未来规划，不是当前实现说明。当前系统仍是本地优先的 Web + SQLite 单机主线。

## 当前与目标差距

| 能力 | 当前状态 | 远期目标 |
| --- | --- | --- |
| 数据库 | SQL.js + SQLite 文件 | 本地/云端可切换的数据访问层 |
| 用户身份 | 无登录体系 | 企业用户登录和会话管理 |
| 权限 | 无 RBAC | 角色、权限、按钮级控制 |
| 审计 | 无统一操作日志 | 金额和状态变更可追溯 |
| 协同 | 单机文件模式 | 多用户并发访问和冲突控制 |

当前 `package.json` 未引入 CloudBase、Pinia、认证 SDK 或后端服务依赖；不要把本文描述当作已实现能力。

## 推荐演进顺序

| 阶段 | 目标 | 说明 |
| --- | --- | --- |
| 1. 稳定本地业务 | 先跑通真实项目周期 | 云端前先把项目、合同、BOQ、结算、收款、发票、报表链路稳定下来。 |
| 2. 抽象数据访问 | 在本地仍可运行的前提下抽象数据库接口 | 不引入云资源，先隔离业务服务 and SQL 执行细节。 |
| 3. 引入身份与权限 | 增加用户、角色、权限表 | 先定义权限模型，再接认证供应商。 |
| 4. 引入审计 | 对关键写操作记录前后快照 | 重点覆盖金额、结算状态、合同/BOQ 变更。 |
| 5. 接入云端数据库 | 替换或扩展数据适配器 | 需要服务端执行 SQL、事务、权限校验和审计写入。 |

## 数据访问抽象候选

```typescript
export interface DatabaseAdapter {
  query<T>(sql: string, params?: unknown[]): Promise<T[]>
  execute(sql: string, params?: unknown[]): Promise<void>
  transaction<T>(action: (adapter: DatabaseAdapter) => Promise<T>): Promise<T>
}
```

候选实现：

| 适配器 | 用途 |
| --- | --- |
| `LocalSqliteAdapter` | 包装当前 SQL.js + SQLite 文件能力。 |
| `RemoteSqlAdapter` | 通过服务端 API 或云函数执行 SQL。 |

不要在业务服务里直接判断“本地/云端”。业务服务只依赖统一接口。

## 认证与权限规划

| 表 | 用途 |
| --- | --- |
| `sys_users` | 用户身份、状态、显示名。 |
| `sys_roles` | 角色定义。 |
| `sys_permissions` | 权限点定义。 |
| `sys_user_roles` | 用户与角色关系。 |
| `sys_role_permissions` | 角色与权限关系。 |

权限粒度示例：

| 权限码 | 说明 |
| --- | --- |
| `project:write` | 新建和编辑项目。 |
| `contract:write` | 新建和编辑合同/BOQ。 |
| `settlement:confirm` | 确认结算。 |
| `settlement:approve` | 审批结算。 |
| `payment:write` | 写入收款记录。 |
| `invoice:write` | 写入发票记录. |
| `data:admin` | 管理数据库位置和备份。 |
| `audit:read` | 查看审计日志。 |

## 审计规划

| 操作 | 审计内容 |
| --- | --- |
| 新建 | 操作者、表、记录 ID、创建内容、时间。 |
| 修改 | 操作者、表、记录 ID、修改前快照、修改后快照、时间。 |
| 删除 | 操作者、表、记录 ID、删除前快照、时间。 |
| 状态变更 | 旧状态、新状态、业务编号、操作者、时间。 |

审计应放在统一写入层或服务端接口层，避免散落在每个页面组件里。

## 暂不做

| 暂不做 | 原因 |
| --- | --- |
| 直接把当前前端改成云端直连数据库 | 权限、审计、事务边界不清晰。 |
| 在业务页面里硬编码云端逻辑 | 会破坏本地/云端双模切换。 |
| 先接登录再补数据权限 | 登录不等于授权，容易留下越权入口。 |
| 把 `/api/db` 当成生产协同接口 | 它当前只是 Vite 开发桥。 |
